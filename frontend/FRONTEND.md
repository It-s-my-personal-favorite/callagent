HERE COMES THE FRONTEND VIA FLUTTER

Lighthouse sinnvoll messen: `lighthouse-admin-release.bat` (Release + CSP + ohne Service-Worker-Register) oder VS Code **„Admin Web (Release, Lighthouse)“** → dann `http://127.0.0.1:8787/` — nicht Port `18091` (Debug). Siehe `DEPLOYMENT.md` §6.1.
